# HybridHopHL
- The code for generating hop labels on the GPU
- Enviroment：cmake 3.8, g++ 4.8.5, cuda 12.4, boost 1.85.0
- CMake can be used for compilation and execution, or you can directly run the test.sh file in the directory.
```
rm -rf build
mkdir build
cd build
cmake ..
make
./bin/Test
```
